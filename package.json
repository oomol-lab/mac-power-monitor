{
  "name": "mac-power-monitor",
  "version": "1.0.0",
  "author": "Tao Zeyu",
  "type": "commonjs",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "napi": {
    "name": "mac-power-monitor"
  },
  "files": ["lib"],
  "scripts": {
    "clean": "cargo clean && rimraf lib",
    "build": "pnpm clean && napi build --release lib && tsup-node",
    "build:debug": "pnpm clean && napi build lib && tsup-node"
  },
  "devDependencies": {
    "@napi-rs/cli": "^1.0.0",
    "@types/node": "^20.8.8",
    "rimraf": "^5.0.5",
    "tsup": "^7.2.0",
    "typescript": "^5.2.2"
  }
}