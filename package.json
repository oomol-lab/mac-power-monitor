{
    "package": "mac-power-monitor",
    "version": "1.0.0",
    "author": "Tao Zeyu",
    "napi": {
        "name": "mac-power-monitor"
    },
    "scripts": {
        "clean": "cargo clean && rimraf lib",
        "build": "pnpm clean && napi build --release lib && tsup-node",
        "build:debug": "pnpm clean && napi build lib && tsup-node"
    },
    "devDependencies": {
        "@napi-rs/cli": "^1.0.0",
        "@types/node": "^20.8.8",
        "rimraf": "^5.0.5",
        "tsup": "^7.2.0",
        "typescript": "^5.2.2"
    }
}