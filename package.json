{
    "package": "mac-power-monitor",
    "devDependencies": {
        "@napi-rs/cli": "^1.0.0",
        "rimraf": "^5.0.5"
    },
    "napi": {
        "name": "mac-power-monitor"
    },
    "scripts": {
        "build": "cargo clean && napi build --release dist",
        "build:debug": "cargo clean && napi build dist"
    }
}